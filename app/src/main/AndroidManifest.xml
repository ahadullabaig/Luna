<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      tools:ignore="DataExtractionRules" — lint asks for android:fullBackupContent as well,
      because dataExtractionRules only applies from Android 12 and minSdk is 26. That advice
      assumes allowBackup is true. It is false, which disables backup outright below
      Android 12, so fullBackupContent would never be read. A rules file that is never
      consulted is worse than none: it looks like the thing protecting the data.
    -->
    <application
        android:name=".LunaApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:ignore="DataExtractionRules"
        android:icon="@mipmap/ic_launcher"
        android:label="Luna"
        android:supportsRtl="true"
        android:theme="@style/Theme.Luna">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Luna">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
